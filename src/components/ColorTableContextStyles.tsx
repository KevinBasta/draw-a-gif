import styled from "styled-components";

export const ColorTableWrapper = styled.div`
    width: inherit;
    height: inherit;
    height: 10vh;
    background-color: grey;

    display: flex;
    flex-direction: row;
    flex-wrap: nowrap;
`;
